import { newProblems } from "./new-problems";
import type { Problem } from '@/types';

export const problems: Problem[] = [...newProblems, 
  { id: "prob-1", number: 1, name: "Two Sum", difficulty: "Easy", topic: "Arrays", pattern: "Hashing", topicId: "p14-t2", subtopic: "Two-sum pattern", prerequisites: [], similarProblems: ["prob-167","prob-15"], tags: ["array","hash-table"], isPremium: false, link: "https://leetcode.com/problems/two-sum/", hints: ["Try a hash map to store seen values","For each element, check if target - element exists in map","Return indices when found"], optimalApproach: "Use a hash map to store each element's index. For each element, check if target - element is in the map. If so, return both indices.", timeComplexity: "O(n)", spaceComplexity: "O(n)", solutions: { java: "class Solution {\n  public int[] twoSum(int[] nums, int target) {\n    Map<Integer, Integer> map = new HashMap<>();\n    for (int i = 0; i < nums.length; i++) {\n      int complement = target - nums[i];\n      if (map.containsKey(complement)) return new int[]{map.get(complement), i};\n      map.put(nums[i], i);\n    }\n    return new int[]{};\n  }\n}", cpp: "class Solution {\npublic:\n  vector<int> twoSum(vector<int>& nums, int target) {\n    unordered_map<int,int> mp;\n    for (int i = 0; i < nums.size(); i++) {\n      int comp = target - nums[i];\n      if (mp.count(comp)) return {mp[comp], i};\n      mp[nums[i]] = i;\n    }\n    return {};\n  }\n};", python: "class Solution:\n  def twoSum(self, nums, target):\n    seen = {}\n    for i, n in enumerate(nums):\n      if target - n in seen:\n        return [seen[target - n], i]\n      seen[n] = i", c: "int* twoSum(int* nums, int numsSize, int target, int* returnSize){\n  int* result = malloc(2 * sizeof(int));\n  for (int i = 0; i < numsSize; i++)\n    for (int j = i+1; j < numsSize; j++)\n      if (nums[i]+nums[j] == target) { result[0]=i; result[1]=j; *returnSize=2; return result; }\n  *returnSize = 0; return result;\n}", javascript: "var twoSum = function(nums, target) {\n  const map = new Map();\n  for (let i = 0; i < nums.length; i++) {\n    if (map.has(target - nums[i])) return [map.get(target - nums[i]), i];\n    map.set(nums[i], i);\n  }\n};", go: "func twoSum(nums []int, target int) []int {\n  m := map[int]int{}\n  for i, n := range nums {\n    if j, ok := m[target-n]; ok { return []int{j, i} }\n    m[n] = i\n  }\n  return nil\n}", csharp: "public class Solution {\n  public int[] TwoSum(int[] nums, int target) {\n    var dict = new Dictionary<int,int>();\n    for (int i = 0; i < nums.Length; i++) {\n      if (dict.TryGetValue(target-nums[i], out int j)) return new[]{j,i};\n      dict[nums[i]] = i;\n    }\n    return new int[0];\n  }\n}", kotlin: "class Solution {\n  fun twoSum(nums: IntArray, target: Int): IntArray {\n    val map = mutableMapOf<Int,Int>()\n    for (i in nums.indices) {\n      val comp = target - nums[i]\n      if (comp in map) return intArrayOf(map[comp]!!, i)\n      map[nums[i]] = i\n    }\n    return intArrayOf()\n  }\n}", rust: "impl Solution {\n  pub fn two_sum(nums: Vec<i32>, target: i32) -> Vec<i32> {\n    let mut map = std::collections::HashMap::new();\n    for (i, &n) in nums.iter().enumerate() {\n      if let Some(&j) = map.get(&(target - n)) { return vec![j as i32, i as i32]; }\n      map.insert(n, i);\n    }\n    vec![]\n  }\n}" } },
  { id: "prob-121", number: 121, name: "Best Time to Buy and Sell Stock", difficulty: "Easy", topic: "Arrays", pattern: "Sliding Window", topicId: "p17-t2", subtopic: "Variable window sum", prerequisites: [], similarProblems: ["prob-122","prob-123"], tags: ["array","dynamic-programming"], isPremium: false, link: "https://leetcode.com/problems/best-time-to-buy-and-sell-stock/", hints: ["Track minimum price so far","Calculate profit at each day","Keep maximum profit"], optimalApproach: "Track the minimum price seen so far. At each day, calculate profit as current price minus min price, and update max profit.", timeComplexity: "O(n)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public int maxProfit(int[] prices) {\n    int minPrice = Integer.MAX_VALUE, maxProfit = 0;\n    for (int price : prices) {\n      minPrice = Math.min(minPrice, price);\n      maxProfit = Math.max(maxProfit, price - minPrice);\n    }\n    return maxProfit;\n  }\n}", cpp: "class Solution {\npublic:\n  int maxProfit(vector<int>& prices) {\n    int minPrice = INT_MAX, maxProfit = 0;\n    for (int p : prices) {\n      minPrice = min(minPrice, p);\n      maxProfit = max(maxProfit, p - minPrice);\n    }\n    return maxProfit;\n  }\n};", python: "class Solution:\n  def maxProfit(self, prices):\n    min_price, max_profit = float('inf'), 0\n    for p in prices:\n      min_price = min(min_price, p)\n      max_profit = max(max_profit, p - min_price)\n    return max_profit", javascript: "var maxProfit = function(prices) {\n  let minPrice = Infinity, maxProfit = 0;\n  for (let p of prices) {\n    minPrice = Math.min(minPrice, p);\n    maxProfit = Math.max(maxProfit, p - minPrice);\n  }\n  return maxProfit;\n};", c: "int maxProfit(int* prices, int pricesSize){\n  int minPrice = INT_MAX, profit = 0;\n  for(int i=0; i<pricesSize; i++){\n    if(prices[i]<minPrice) minPrice=prices[i];\n    if(prices[i]-minPrice>profit) profit=prices[i]-minPrice;\n  }\n  return profit;\n}", go: "func maxProfit(prices []int) int {\n  minPrice, profit := math.MaxInt32, 0\n  for _, p := range prices {\n    if p < minPrice { minPrice = p }\n    if p - minPrice > profit { profit = p - minPrice }\n  }\n  return profit\n}", csharp: "public class Solution {\n  public int MaxProfit(int[] prices) {\n    int minPrice = int.MaxValue, maxProfit = 0;\n    foreach (var p in prices) {\n      minPrice = Math.Min(minPrice, p);\n      maxProfit = Math.Max(maxProfit, p - minPrice);\n    }\n    return maxProfit;\n  }\n}", kotlin: "class Solution {\n  fun maxProfit(prices: IntArray): Int {\n    var minPrice = Int.MAX_VALUE; var maxProfit = 0\n    for (p in prices) { minPrice = minOf(minPrice, p); maxProfit = maxOf(maxProfit, p - minPrice) }\n    return maxProfit\n  }\n}", rust: "impl Solution {\n  pub fn max_profit(prices: Vec<i32>) -> i32 {\n    let mut min_price = i32::MAX; let mut max_profit = 0;\n    for p in prices { min_price = min_price.min(p); max_profit = max_profit.max(p - min_price); }\n    max_profit\n  }\n}" } },
  { id: "prob-217", number: 217, name: "Contains Duplicate", difficulty: "Easy", topic: "Hashing", pattern: "Hash Set", topicId: "p14-t1", subtopic: "Hash set operations", prerequisites: [], similarProblems: ["prob-219","prob-220"], tags: ["array","hash-table"], isPremium: false, link: "https://leetcode.com/problems/contains-duplicate/", hints: ["Use a hash set","Add elements and check for duplicates","Return true if any element already exists"], optimalApproach: "Use a hash set to track seen elements. If any element is already in the set, return true.", timeComplexity: "O(n)", spaceComplexity: "O(n)", solutions: { java: "class Solution {\n  public boolean containsDuplicate(int[] nums) {\n    Set<Integer> set = new HashSet<>();\n    for (int n : nums) { if (!set.add(n)) return true; }\n    return false;\n  }\n}", cpp: "class Solution {\npublic:\n  bool containsDuplicate(vector<int>& nums) {\n    unordered_set<int> s(nums.begin(), nums.end());\n    return s.size() < nums.size();\n  }\n};", python: "class Solution:\n  def containsDuplicate(self, nums):\n    return len(set(nums)) != len(nums)", javascript: "var containsDuplicate = function(nums) {\n  const set = new Set(nums);\n  return set.size !== nums.length;\n};", c: "int cmp(const void* a, const void* b) { return *(int*)a - *(int*)b; }\nbool containsDuplicate(int* nums, int numsSize){\n  qsort(nums, numsSize, sizeof(int), cmp);\n  for(int i=1; i<numsSize; i++) if(nums[i]==nums[i-1]) return true;\n  return false;\n}", go: "func containsDuplicate(nums []int) bool {\n  s := map[int]bool{}\n  for _, n := range nums { if s[n] { return true }; s[n] = true }\n  return false\n}", csharp: "public class Solution {\n  public bool ContainsDuplicate(int[] nums) => new HashSet<int>(nums).Count < nums.Length;\n}", kotlin: "class Solution {\n  fun containsDuplicate(nums: IntArray) = nums.toSet().size < nums.size\n}", rust: "use std::collections::HashSet;\nimpl Solution {\n  pub fn contains_duplicate(nums: Vec<i32>) -> bool {\n    let mut set = HashSet::new();\n    for n in nums { if !set.insert(n) { return true } }\n    false\n  }\n}" } },
  { id: "prob-21", number: 21, name: "Merge Two Sorted Lists", difficulty: "Easy", topic: "Linked Lists", pattern: "Two Pointers", topicId: "p19-t2", subtopic: "Merge sorted lists", prerequisites: [], similarProblems: ["prob-23","prob-88"], tags: ["linked-list","recursion"], isPremium: false, link: "https://leetcode.com/problems/merge-two-sorted-lists/", hints: ["Use a dummy head node","Compare values and link the smaller one","Advance the pointer of the chosen list"], optimalApproach: "Use a dummy node. Compare heads of both lists, link the smaller one, and advance. At the end, attach remaining nodes.", timeComplexity: "O(n+m)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public ListNode mergeTwoLists(ListNode l1, ListNode l2) {\n    ListNode dummy = new ListNode(0), cur = dummy;\n    while (l1 != null && l2 != null) {\n      if (l1.val <= l2.val) { cur.next = l1; l1 = l1.next; }\n      else { cur.next = l2; l2 = l2.next; }\n      cur = cur.next;\n    }\n    cur.next = l1 != null ? l1 : l2;\n    return dummy.next;\n  }\n}", cpp: "class Solution {\npublic:\n  ListNode* mergeTwoLists(ListNode* l1, ListNode* l2) {\n    ListNode dummy(0), *cur = &dummy;\n    while (l1 && l2) {\n      if (l1->val <= l2->val) { cur->next = l1; l1 = l1->next; }\n      else { cur->next = l2; l2 = l2->next; }\n      cur = cur->next;\n    }\n    cur->next = l1 ? l1 : l2;\n    return dummy.next;\n  }\n};", python: "class Solution:\n  def mergeTwoLists(self, l1, l2):\n    dummy = ListNode(0)\n    cur = dummy\n    while l1 and l2:\n      if l1.val <= l2.val: cur.next, l1 = l1, l1.next\n      else: cur.next, l2 = l2, l2.next\n      cur = cur.next\n    cur.next = l1 or l2\n    return dummy.next", javascript: "var mergeTwoLists = function(l1, l2) {\n  const dummy = new ListNode(0); let cur = dummy;\n  while (l1 && l2) {\n    if (l1.val <= l2.val) { cur.next = l1; l1 = l1.next; }\n    else { cur.next = l2; l2 = l2.next; }\n    cur = cur.next;\n  }\n  cur.next = l1 || l2;\n  return dummy.next;\n};", go: "func mergeTwoLists(l1 *ListNode, l2 *ListNode) *ListNode {\n  dummy := &ListNode{}; cur := dummy\n  for l1 != nil && l2 != nil {\n    if l1.Val <= l2.Val { cur.Next = l1; l1 = l1.Next } else { cur.Next = l2; l2 = l2.Next }\n    cur = cur.Next\n  }\n  if l1 != nil { cur.Next = l1 } else { cur.Next = l2 }\n  return dummy.Next\n}", kotlin: "class Solution {\n  fun mergeTwoLists(l1: ListNode?, l2: ListNode?): ListNode? {\n    val dummy = ListNode(0); var cur = dummy\n    var p1 = l1; var p2 = l2\n    while (p1 != null && p2 != null) {\n      if (p1.`val` <= p2.`val`) { cur.next = p1; p1 = p1.next } else { cur.next = p2; p2 = p2.next }\n      cur = cur.next!!\n    }\n    cur.next = p1 ?: p2\n    return dummy.next\n  }\n}", csharp: "public class Solution {\n  public ListNode MergeTwoLists(ListNode l1, ListNode l2) {\n    var dummy = new ListNode(0); var cur = dummy;\n    while (l1 != null && l2 != null) {\n      if (l1.val <= l2.val) { cur.next = l1; l1 = l1.next; }\n      else { cur.next = l2; l2 = l2.next; }\n      cur = cur.next;\n    }\n    cur.next = l1 ?? l2;\n    return dummy.next;\n  }\n}", rust: "impl Solution {\n  pub fn merge_two_lists(l1: Option<Box<ListNode>>, l2: Option<Box<ListNode>>) -> Option<Box<ListNode>> {\n    match (l1, l2) {\n      (None, Some(b)) | (Some(b), None) => Some(b),\n      (Some(mut a), Some(mut b)) => {\n        if a.val <= b.val { a.next = Self::merge_two_lists(a.next.take(), Some(b)); Some(a) }\n        else { b.next = Self::merge_two_lists(Some(a), b.next.take()); Some(b) }\n      }\n      (None, None) => None\n    }\n  }\n}", c: "struct ListNode* mergeTwoLists(struct ListNode* l1, struct ListNode* l2){\n  struct ListNode dummy = {0, NULL}, *cur = &dummy;\n  while (l1 && l2) {\n    if (l1->val <= l2->val) { cur->next = l1; l1 = l1->next; }\n    else { cur->next = l2; l2 = l2->next; }\n    cur = cur->next;\n  }\n  cur->next = l1 ? l1 : l2;\n  return dummy.next;\n}" } },
  { id: "prob-206", number: 206, name: "Reverse Linked List", difficulty: "Easy", topic: "Linked Lists", pattern: "Pointers", topicId: "p19-t2", subtopic: "Reverse linked list", prerequisites: [], similarProblems: ["prob-92","prob-25"], tags: ["linked-list","recursion"], isPremium: false, link: "https://leetcode.com/problems/reverse-linked-list/", hints: ["Use three pointers: prev, cur, next","Reverse the link at each step","Move all pointers forward"], optimalApproach: "Iterate with prev, cur, next pointers. At each step, point cur->next to prev, then advance all pointers.", timeComplexity: "O(n)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public ListNode reverseList(ListNode head) {\n    ListNode prev = null;\n    while (head != null) { ListNode next = head.next; head.next = prev; prev = head; head = next; }\n    return prev;\n  }\n}", cpp: "class Solution {\npublic:\n  ListNode* reverseList(ListNode* head) {\n    ListNode* prev = nullptr;\n    while (head) { ListNode* next = head->next; head->next = prev; prev = head; head = next; }\n    return prev;\n  }\n};", python: "class Solution:\n  def reverseList(self, head):\n    prev = None\n    while head:\n      nxt = head.next\n      head.next = prev\n      prev = head\n      head = nxt\n    return prev", javascript: "var reverseList = function(head) {\n  let prev = null;\n  while (head) { let next = head.next; head.next = prev; prev = head; head = next; }\n  return prev;\n};", c: "struct ListNode* reverseList(struct ListNode* head){\n  struct ListNode* prev = NULL;\n  while (head) { struct ListNode* next = head->next; head->next = prev; prev = head; head = next; }\n  return prev;\n}", go: "func reverseList(head *ListNode) *ListNode {\n  var prev *ListNode\n  for head != nil { next := head.Next; head.Next = prev; prev = head; head = next }\n  return prev\n}", csharp: "public class Solution {\n  public ListNode ReverseList(ListNode head) {\n    ListNode prev = null;\n    while (head != null) { var next = head.next; head.next = prev; prev = head; head = next; }\n    return prev;\n  }\n}", kotlin: "class Solution {\n  fun reverseList(head: ListNode?): ListNode? {\n    var prev: ListNode? = null; var cur = head\n    while (cur != null) { val next = cur.next; cur.next = prev; prev = cur; cur = next }\n    return prev\n  }\n}", rust: "impl Solution {\n  pub fn reverse_list(head: Option<Box<ListNode>>) -> Option<Box<ListNode>> {\n    let mut prev = None; let mut cur = head;\n    while let Some(mut node) = cur { let next = node.next.take(); node.next = prev; prev = Some(node); cur = next; }\n    prev\n  }\n}" } },
  { id: "prob-20", number: 20, name: "Valid Parentheses", difficulty: "Easy", topic: "Stack & Queue", pattern: "Stack", topicId: "p20-t1", subtopic: "Balanced parentheses", prerequisites: [], similarProblems: ["prob-22","prob-32"], tags: ["string","stack"], isPremium: false, link: "https://leetcode.com/problems/valid-parentheses/", hints: ["Use a stack","Push opening brackets","For closing brackets, check if stack top matches"], optimalApproach: "Use a stack. Push opening brackets, pop for closing brackets and check if they match. Stack should be empty at the end.", timeComplexity: "O(n)", spaceComplexity: "O(n)", solutions: { java: "class Solution {\n  public boolean isValid(String s) {\n    Stack<Character> st = new Stack<>();\n    for (char c : s.toCharArray()) {\n      if (c=='(') st.push(')'); else if (c=='{') st.push('}'); else if (c=='[') st.push(']');\n      else if (st.isEmpty() || st.pop() != c) return false;\n    }\n    return st.isEmpty();\n  }\n}", cpp: "class Solution {\npublic:\n  bool isValid(string s) {\n    stack<char> st;\n    for (char c : s) {\n      if (c=='(') st.push(')'); else if (c=='{') st.push('}'); else if (c=='[') st.push(']');\n      else if (st.empty() || st.top()!=c) return false; else st.pop();\n    }\n    return st.empty();\n  }\n};", python: "class Solution:\n  def isValid(self, s: str) -> bool:\n    st = []\n    pairs = {'(':')','{':'}','[':']'}\n    for c in s:\n      if c in pairs: st.append(pairs[c])\n      elif not st or st.pop() != c: return False\n    return not st", javascript: "var isValid = function(s) {\n  const st = [];\n  for (const c of s) {\n    if (c==='(') st.push(')'); else if (c==='{') st.push('}'); else if (c==='[') st.push(']');\n    else if (!st.length || st.pop()!==c) return false;\n  }\n  return st.length === 0;\n};", c: "bool isValid(char* s){\n  char st[10000]; int top=-1;\n  for(int i=0; s[i]; i++){\n    if(s[i]=='('||s[i]=='{'||s[i]=='[') st[++top]=s[i];\n    else { if(top<0) return false; char o=st[top--];\n      if((s[i]==')'&&o!='(')||(s[i]=='}'&&o!='{')||(s[i]==']'&&o!='[')) return false; }\n  }\n  return top<0;\n}", go: "func isValid(s string) bool {\n  var st []rune\n  pairs := map[rune]rune{'(':')','{':'}','[':']'}\n  for _, c := range s {\n    if v, ok := pairs[c]; ok { st = append(st, v) } else {\n      if len(st)==0 || st[len(st)-1]!=c { return false }\n      st = st[:len(st)-1]\n    }\n  }\n  return len(st)==0\n}", kotlin: "class Solution {\n  fun isValid(s: String): Boolean {\n    val st = ArrayDeque<Char>()\n    for (c in s) when(c) {\n      '('->st.addLast(')'); '{'->st.addLast('}'); '['->st.addLast(']')\n      else -> { if (st.isEmpty() || st.removeLast()!=c) return false }\n    }\n    return st.isEmpty()\n  }\n}", csharp: "public class Solution {\n  public bool IsValid(string s) {\n    var st = new Stack<char>();\n    foreach (var c in s) {\n      if (c=='(') st.Push(')'); else if (c=='{') st.Push('}'); else if (c=='[') st.Push(']');\n      else if (st.Count==0 || st.Pop()!=c) return false;\n    }\n    return st.Count == 0;\n  }\n}", rust: "impl Solution {\n  pub fn is_valid(s: String) -> bool {\n    let mut st = vec![];\n    for c in s.chars() {\n      match c { '(' => st.push(')'), '{' => st.push('}'), '[' => st.push(']'),\n        _ => { if st.pop() != Some(c) { return false } } }\n    }\n    st.is_empty()\n  }\n}" } },
  { id: "prob-704", number: 704, name: "Binary Search", difficulty: "Easy", topic: "Searching", pattern: "Binary Search", topicId: "p13-t1", subtopic: "Binary search", prerequisites: [], similarProblems: ["prob-278","prob-35"], tags: ["array","binary-search"], isPremium: false, link: "https://leetcode.com/problems/binary-search/", hints: ["Set left and right pointers","Calculate mid, compare with target","Adjust search space based on comparison"], optimalApproach: "Standard binary search: set left=0, right=n-1, calculate mid, compare, and narrow search space by half each iteration.", timeComplexity: "O(log n)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public int search(int[] nums, int target) {\n    int l = 0, r = nums.length - 1;\n    while (l <= r) { int m = l + (r-l)/2;\n      if (nums[m] == target) return m; else if (nums[m] < target) l = m+1; else r = m-1; }\n    return -1;\n  }\n}", cpp: "class Solution {\npublic:\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = nums.size()-1;\n    while (l <= r) { int m = l+(r-l)/2;\n      if (nums[m]==target) return m; else if (nums[m]<target) l=m+1; else r=m-1; }\n    return -1;\n  }\n};", python: "class Solution:\n  def search(self, nums, target):\n    l, r = 0, len(nums)-1\n    while l <= r:\n      m = l + (r-l)//2\n      if nums[m] == target: return m\n      elif nums[m] < target: l = m+1\n      else: r = m-1\n    return -1", javascript: "var search = function(nums, target) {\n  let l = 0, r = nums.length-1;\n  while (l <= r) { const m = Math.floor(l+(r-l)/2);\n    if (nums[m]===target) return m; else if (nums[m]<target) l=m+1; else r=m-1; }\n  return -1;\n};", c: "int search(int* nums, int numsSize, int target){\n  int l=0, r=numsSize-1;\n  while(l<=r){ int m=l+(r-l)/2;\n    if(nums[m]==target) return m; else if(nums[m]<target) l=m+1; else r=m-1; }\n  return -1;\n}", go: "func search(nums []int, target int) int {\n  l, r := 0, len(nums)-1\n  for l <= r { m := l+(r-l)/2\n    if nums[m]==target { return m } else if nums[m]<target { l=m+1 } else { r=m-1 } }\n  return -1\n}", csharp: "public class Solution {\n  public int Search(int[] nums, int target) {\n    int l=0, r=nums.Length-1;\n    while (l<=r) { int m=l+(r-l)/2;\n      if (nums[m]==target) return m; else if (nums[m]<target) l=m+1; else r=m-1; }\n    return -1;\n  }\n}", kotlin: "class Solution {\n  fun search(nums: IntArray, target: Int): Int {\n    var l=0; var r=nums.lastIndex\n    while (l<=r) { val m=l+(r-l)/2\n      when { nums[m]==target -> return m; nums[m]<target -> l=m+1; else -> r=m-1 } }\n    return -1\n  }\n}", rust: "impl Solution {\n  pub fn search(nums: Vec<i32>, target: i32) -> i32 {\n    let (mut l, mut r) = (0, nums.len() as i32 - 1);\n    while l <= r { let m = l + (r-l)/2;\n      match nums[m as usize].cmp(&target) { Equal => return m, Less => l=m+1, Greater => r=m-1 } }\n    -1\n  }\n}" } },
  { id: "prob-53", number: 53, name: "Maximum Subarray", difficulty: "Medium", topic: "Subarrays", pattern: "Kadane's Algorithm", topicId: "p15-t1", subtopic: "Maximum subarray", prerequisites: [], similarProblems: ["prob-152","prob-918"], tags: ["array","dynamic-programming","divide-and-conquer"], isPremium: false, link: "https://leetcode.com/problems/maximum-subarray/", hints: ["Track current sum and max sum","If current sum goes negative, reset to 0","Compare at each step"], optimalApproach: "Kadane's Algorithm: track current sum. If current sum becomes negative, reset to 0. Track maximum sum seen.", timeComplexity: "O(n)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public int maxSubArray(int[] nums) {\n    int maxSum = nums[0], curSum = 0;\n    for (int n : nums) { curSum = Math.max(n, curSum + n); maxSum = Math.max(maxSum, curSum); }\n    return maxSum;\n  }\n}", cpp: "class Solution {\npublic:\n  int maxSubArray(vector<int>& nums) {\n    int maxSum = nums[0], curSum = 0;\n    for (int n : nums) { curSum = max(n, curSum+n); maxSum = max(maxSum, curSum); }\n    return maxSum;\n  }\n};", python: "class Solution:\n  def maxSubArray(self, nums):\n    maxSum = curSum = nums[0]\n    for n in nums[1:]:\n      curSum = max(n, curSum + n)\n      maxSum = max(maxSum, curSum)\n    return maxSum", javascript: "var maxSubArray = function(nums) {\n  let maxSum = nums[0], curSum = 0;\n  for (const n of nums) { curSum = Math.max(n, curSum+n); maxSum = Math.max(maxSum, curSum); }\n  return maxSum;\n};", c: "int maxSubArray(int* nums, int numsSize){\n  int maxSum=nums[0], curSum=0;\n  for(int i=0; i<numsSize; i++){ curSum = nums[i] > curSum+nums[i] ? nums[i] : curSum+nums[i]; if(curSum>maxSum) maxSum=curSum; }\n  return maxSum;\n}", go: "func maxSubArray(nums []int) int {\n  maxSum, curSum := nums[0], 0\n  for _, n := range nums { curSum = max(n, curSum+n); maxSum = max(maxSum, curSum) }\n  return maxSum\n}", csharp: "public class Solution {\n  public int MaxSubArray(int[] nums) {\n    int maxSum = nums[0], curSum = 0;\n    foreach (var n in nums) { curSum = Math.Max(n, curSum+n); maxSum = Math.Max(maxSum, curSum); }\n    return maxSum;\n  }\n}", kotlin: "class Solution {\n  fun maxSubArray(nums: IntArray): Int {\n    var maxSum = nums[0]; var curSum = 0\n    for (n in nums) { curSum = maxOf(n, curSum+n); maxSum = maxOf(maxSum, curSum) }\n    return maxSum\n  }\n}", rust: "impl Solution {\n  pub fn max_sub_array(nums: Vec<i32>) -> i32 {\n    let mut max_sum = nums[0]; let mut cur_sum = 0;\n    for n in nums { cur_sum = n.max(cur_sum + n); max_sum = max_sum.max(cur_sum); }\n    max_sum\n  }\n}" } },
  { id: "prob-15", number: 15, name: "3Sum", difficulty: "Medium", topic: "Two Pointers", pattern: "Sort + Two Pointers", prerequisites: ["prob-1"], similarProblems: ["prob-1","prob-16","prob-259"], tags: ["array","two-pointers","sorting"], isPremium: false, link: "https://leetcode.com/problems/3sum/", hints: ["Sort the array first","Fix one element and use two pointers for the other two","Skip duplicates"], optimalApproach: "Sort the array. For each element, use two pointers (left and right) to find pairs that sum to -element. Skip duplicates to avoid repeated triplets.", timeComplexity: "O(n^2)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public List<List<Integer>> threeSum(int[] nums) {\n    Arrays.sort(nums); List<List<Integer>> res = new ArrayList<>();\n    for (int i = 0; i < nums.length-2; i++) {\n      if (i>0 && nums[i]==nums[i-1]) continue;\n      int l = i+1, r = nums.length-1;\n      while (l < r) { int sum = nums[i]+nums[l]+nums[r];\n        if (sum==0) { res.add(Arrays.asList(nums[i],nums[l],nums[r]));\n          while(l<r&&nums[l]==nums[l+1])l++; while(l<r&&nums[r]==nums[r-1])r--; l++; r--; }\n        else if (sum<0) l++; else r--; }\n    }\n    return res;\n  }\n}", cpp: "class Solution {\npublic:\n  vector<vector<int>> threeSum(vector<int>& nums) {\n    sort(nums.begin(),nums.end()); vector<vector<int>> res;\n    for(int i=0; i<(int)nums.size()-2; i++) {\n      if(i>0 && nums[i]==nums[i-1]) continue;\n      int l=i+1, r=nums.size()-1;\n      while(l<r) { int s=nums[i]+nums[l]+nums[r];\n        if(s==0){res.push_back({nums[i],nums[l],nums[r]});\n          while(l<r&&nums[l]==nums[l+1])l++; while(l<r&&nums[r]==nums[r-1])r--; l++;r--;}\n        else if(s<0)l++; else r--;}\n    }\n    return res;\n  }\n};", python: "class Solution:\n  def threeSum(self, nums):\n    nums.sort(); res = []\n    for i in range(len(nums)-2):\n      if i>0 and nums[i]==nums[i-1]: continue\n      l, r = i+1, len(nums)-1\n      while l<r:\n        s = nums[i]+nums[l]+nums[r]\n        if s==0: res.append([nums[i],nums[l],nums[r]]); l+=1; r-=1\n          while l<r and nums[l]==nums[l-1]: l+=1\n          while l<r and nums[r]==nums[r+1]: r-=1\n        elif s<0: l+=1\n        else: r-=1\n    return res", javascript: "var threeSum = function(nums) {\n  nums.sort((a,b)=>a-b); const res=[];\n  for(let i=0; i<nums.length-2; i++){\n    if(i>0 && nums[i]===nums[i-1]) continue;\n    let l=i+1, r=nums.length-1;\n    while(l<r){ const s=nums[i]+nums[l]+nums[r];\n      if(s===0){res.push([nums[i],nums[l],nums[r]]);\n        while(l<r&&nums[l]===nums[l+1])l++; while(l<r&&nums[r]===nums[r-1])r--; l++;r--;}\n      else if(s<0)l++; else r--;}\n  }\n  return res;\n};", go: "func threeSum(nums []int) [][]int {\n  sort.Ints(nums); var res [][]int\n  for i:=0; i<len(nums)-2; i++ {\n    if i>0 && nums[i]==nums[i-1] { continue }\n    l, r := i+1, len(nums)-1\n    for l<r { s:=nums[i]+nums[l]+nums[r]\n      if s==0 { res=append(res,[]int{nums[i],nums[l],nums[r]}); for l<r&&nums[l]==nums[l+1]{l++}; for l<r&&nums[r]==nums[r-1]{r--}; l++;r-- } else if s<0 { l++ } else { r-- } }\n  }\n  return res\n}", kotlin: "class Solution {\n  fun threeSum(nums: IntArray): List<List<Int>> {\n    nums.sort(); val res = mutableListOf<List<Int>>()\n    for (i in 0 until nums.size-2) {\n      if (i>0 && nums[i]==nums[i-1]) continue\n      var l=i+1; var r=nums.size-1\n      while (l<r) { val s=nums[i]+nums[l]+nums[r]\n        if (s==0) { res.add(listOf(nums[i],nums[l],nums[r])); while(l<r&&nums[l]==nums[l+1])l++; while(l<r&&nums[r]==nums[r-1])r--; l++;r-- }\n        else if (s<0) l++ else r-- }\n    }\n    return res\n  }\n}", csharp: "public class Solution {\n  public IList<IList<int>> ThreeSum(int[] nums) {\n    Array.Sort(nums); var res = new List<IList<int>>();\n    for(int i=0; i<nums.Length-2; i++) {\n      if(i>0 && nums[i]==nums[i-1]) continue;\n      int l=i+1, r=nums.Length-1;\n      while(l<r) { int s=nums[i]+nums[l]+nums[r];\n        if(s==0){res.Add(new List<int>{nums[i],nums[l],nums[r]}); while(l<r&&nums[l]==nums[l+1])l++; while(l<r&&nums[r]==nums[r-1])r--; l++;r--;}\n        else if(s<0)l++; else r--;}\n    }\n    return res;\n  }\n}", rust: "impl Solution {\n  pub fn three_sum(mut nums: Vec<i32>) -> Vec<Vec<i32>> {\n    nums.sort(); let mut res = vec![];\n    for i in 0..nums.len().saturating_sub(2) {\n      if i>0 && nums[i]==nums[i-1] { continue }\n      let (mut l, mut r) = (i+1, nums.len()-1);\n      while l<r { let s=nums[i]+nums[l]+nums[r];\n        if s==0 { res.push(vec![nums[i],nums[l],nums[r]]); while l<r&&nums[l]==nums[l+1]{l+=1} while l<r&&nums[r]==nums[r-1]{r-=1} l+=1; r-=1 }\n        else if s<0 { l+=1 } else { r-=1 } }\n    }\n    res\n  }\n}", c: "int cmp(const void*a,const void*b){return *(int*)a-*(int*)b;}\nint** threeSum(int* nums, int numsSize, int* returnSize, int** returnColumnSizes){\n  qsort(nums,numsSize,sizeof(int),cmp); int** res=malloc(100000*sizeof(int*)); int cnt=0;\n  for(int i=0;i<numsSize-2;i++){ if(i>0&&nums[i]==nums[i-1])continue;\n    int l=i+1,r=numsSize-1;\n    while(l<r){int s=nums[i]+nums[l]+nums[r];\n      if(s==0){res[cnt]=malloc(3*sizeof(int));res[cnt][0]=nums[i];res[cnt][1]=nums[l];res[cnt][2]=nums[r];cnt++;\n        while(l<r&&nums[l]==nums[l+1])l++;while(l<r&&nums[r]==nums[r-1])r--;l++;r--;}else if(s<0)l++;else r--;}}\n  *returnSize=cnt;return res;\n}" } },
  { id: "prob-200", number: 200, name: "Number of Islands", difficulty: "Medium", topic: "Graphs", pattern: "BFS/DFS", topicId: "p24-t1", subtopic: "BFS", prerequisites: [], similarProblems: ["prob-695","prob-130","prob-463"], tags: ["array","dfs","bfs","union-find","matrix"], isPremium: false, link: "https://leetcode.com/problems/number-of-islands/", hints: ["Traverse the grid","When you find '1', do DFS/BFS to mark connected land","Count the number of times you start a new DFS"], optimalApproach: "Iterate through the grid. When you find a '1', increment count and do DFS/BFS to mark all connected land as visited (set to '0').", timeComplexity: "O(m*n)", spaceComplexity: "O(m*n)", solutions: { java: "class Solution {\n  public int numIslands(char[][] grid) {\n    int count = 0;\n    for (int i = 0; i < grid.length; i++)\n      for (int j = 0; j < grid[0].length; j++)\n        if (grid[i][j]=='1') { count++; dfs(grid, i, j); }\n    return count;\n  }\n  void dfs(char[][] g, int i, int j) {\n    if (i<0||j<0||i>=g.length||j>=g[0].length||g[i][j]!='1') return;\n    g[i][j]='0'; dfs(g,i+1,j); dfs(g,i-1,j); dfs(g,i,j+1); dfs(g,i,j-1);\n  }\n}", cpp: "class Solution {\npublic:\n  int numIslands(vector<vector<char>>& grid) {\n    int count=0;\n    for(int i=0;i<grid.size();i++)\n      for(int j=0;j<grid[0].length();j++)\n        if(grid[i][j]=='1'){count++;dfs(grid,i,j);}\n    return count;\n  }\n  void dfs(vector<vector<char>>&g,int i,int j){\n    if(i<0||j<0||i>=g.size()||j>=g[0].size()||g[i][j]!='1')return;\n    g[i][j]='0';dfs(g,i+1,j);dfs(g,i-1,j);dfs(g,i,j+1);dfs(g,i,j-1);\n  }\n};", python: "class Solution:\n  def numIslands(self, grid):\n    count = 0\n    for i in range(len(grid)):\n      for j in range(len(grid[0])):\n        if grid[i][j]=='1':\n          count += 1\n          self.dfs(grid, i, j)\n    return count\n  def dfs(self, g, i, j):\n    if i<0 or j<0 or i>=len(g) or j>=len(g[0]) or g[i][j]!='1': return\n    g[i][j]='0'\n    self.dfs(g,i+1,j); self.dfs(g,i-1,j); self.dfs(g,i,j+1); self.dfs(g,i,j-1)", javascript: "var numIslands = function(grid) {\n  let count = 0;\n  for (let i=0; i<grid.length; i++)\n    for (let j=0; j<grid[0].length; j++)\n      if (grid[i][j]==='1') { count++; dfs(grid, i, j); }\n  return count;\n  function dfs(g, i, j) {\n    if (i<0||j<0||i>=g.length||j>=g[0].length||g[i][j]!=='1') return;\n    g[i][j]='0'; dfs(g,i+1,j); dfs(g,i-1,j); dfs(g,i,j+1); dfs(g,i,j-1);\n  }\n};", c: "void dfs(char** g, int i, int j, int m, int n) {\n  if (i<0||j<0||i>=m||j>=n||g[i][j]!='1') return;\n  g[i][j]='0'; dfs(g,i+1,j,m,n); dfs(g,i-1,j,m,n); dfs(g,i,j+1,m,n); dfs(g,i,j-1,m,n);\n}\nint numIslands(char** grid, int gridSize, int* gridColSize){\n  int count=0;\n  for(int i=0;i<gridSize;i++) for(int j=0;j<gridColSize[i];j++) if(grid[i][j]=='1'){count++;dfs(grid,i,j,gridSize,gridColSize[i]);}\n  return count;\n}", go: "func numIslands(grid [][]byte) int {\n  count := 0\n  var dfs func(i, j int)\n  dfs = func(i, j int) {\n    if i<0||j<0||i>=len(grid)||j>=len(grid[0])||grid[i][j]!='1' { return }\n    grid[i][j]='0'; dfs(i+1,j); dfs(i-1,j); dfs(i,j+1); dfs(i,j-1)\n  }\n  for i:=0; i<len(grid); i++ { for j:=0; j<len(grid[0]); j++ { if grid[i][j]=='1' { count++; dfs(i,j) } } }\n  return count\n}", kotlin: "class Solution {\n  fun numIslands(grid: Array<CharArray>): Int {\n    var count = 0\n    for (i in grid.indices) for (j in grid[0].indices) if (grid[i][j]=='1') { count++; dfs(grid, i, j) }\n    return count\n  }\n  fun dfs(g: Array<CharArray>, i: Int, j: Int) {\n    if (i<0||j<0||i>=g.size||j>=g[0].size||g[i][j]!='1') return\n    g[i][j]='0'; dfs(g,i+1,j); dfs(g,i-1,j); dfs(g,i,j+1); dfs(g,i,j-1)\n  }\n}", csharp: "public class Solution {\n  public int NumIslands(char[][] grid) {\n    int count = 0;\n    for (int i=0; i<grid.Length; i++)\n      for (int j=0; j<grid[0].Length; j++)\n        if (grid[i][j]=='1') { count++; Dfs(grid, i, j); }\n    return count;\n  }\n  void Dfs(char[][] g, int i, int j) {\n    if (i<0||j<0||i>=g.Length||j>=g[0].Length||g[i][j]!='1') return;\n    g[i][j]='0'; Dfs(g,i+1,j); Dfs(g,i-1,j); Dfs(g,i,j+1); Dfs(g,i,j-1);\n  }\n}", rust: "impl Solution {\n  pub fn num_islands(grid: &mut Vec<Vec<char>>) -> i32 {\n    let mut count = 0;\n    for i in 0..grid.len() { for j in 0..grid[0].len() {\n      if grid[i][j]=='1' { count += 1; Self::dfs(grid, i as i32, j as i32); }\n    }}\n    count\n  }\n  fn dfs(g: &mut Vec<Vec<char>>, i: i32, j: i32) {\n    if i<0||j<0||i>=g.len() as i32||j>=g[0].len() as i32||g[i as usize][j as usize]!='1' { return }\n    g[i as usize][j as usize]='0';\n    Self::dfs(g,i+1,j); Self::dfs(g,i-1,j); Self::dfs(g,i,j+1); Self::dfs(g,i,j-1);\n  }\n}" } },
  { id: "prob-3", number: 3, name: "Longest Substring Without Repeating Characters", difficulty: "Medium", topic: "Sliding Window", pattern: "Sliding Window + Hash Set", topicId: "p17-t2", subtopic: "Longest substring", prerequisites: [], similarProblems: ["prob-159","prob-340"], tags: ["hash-table","string","sliding-window"], isPremium: false, link: "https://leetcode.com/problems/longest-substring-without-repeating-characters/", hints: ["Use a hash set to track characters in current window","Expand right, shrink left when duplicate found","Track max length"], optimalApproach: "Use a sliding window with a hash set. Expand right pointer, and when a duplicate is found, shrink left pointer until the duplicate is removed.", timeComplexity: "O(n)", spaceComplexity: "O(min(n, charset))", solutions: { java: "class Solution {\n  public int lengthOfLongestSubstring(String s) {\n    Set<Character> set = new HashSet<>();\n    int l = 0, max = 0;\n    for (int r = 0; r < s.length(); r++) {\n      while (set.contains(s.charAt(r))) set.remove(s.charAt(l++));\n      set.add(s.charAt(r)); max = Math.max(max, r-l+1);\n    }\n    return max;\n  }\n}", cpp: "class Solution {\npublic:\n  int lengthOfLongestSubstring(string s) {\n    unordered_set<char> st; int l=0, mx=0;\n    for(int r=0; r<s.size(); r++) {\n      while(st.count(s[r])) st.erase(s[l++]);\n      st.insert(s[r]); mx=max(mx, r-l+1);\n    }\n    return mx;\n  }\n};", python: "class Solution:\n  def lengthOfLongestSubstring(self, s: str) -> int:\n    seen = set(); l = 0; mx = 0\n    for r in range(len(s)):\n      while s[r] in seen: seen.remove(s[l]); l += 1\n      seen.add(s[r]); mx = max(mx, r-l+1)\n    return mx", javascript: "var lengthOfLongestSubstring = function(s) {\n  const set = new Set(); let l = 0, max = 0;\n  for (let r = 0; r < s.length; r++) {\n    while (set.has(s[r])) set.delete(s[l++]);\n    set.add(s[r]); max = Math.max(max, r-l+1);\n  }\n  return max;\n};", c: "int lengthOfLongestSubstring(char * s){\n  int l=0, mx=0, n=strlen(s); int idx[128]={0};\n  for(int r=0; r<n; r++){\n    if(idx[s[r]]>l) l=idx[s[r]];\n    idx[s[r]]=r+1; mx=mx>r-l+1?mx:r-l+1;\n  }\n  return mx;\n}", go: "func lengthOfLongestSubstring(s string) int {\n  m := map[byte]int{}; l, mx := 0, 0\n  for r := 0; r < len(s); r++ {\n    if v, ok := m[s[r]]; ok && v >= l { l = v + 1 }\n    m[s[r]] = r; if r-l+1 > mx { mx = r-l+1 }\n  }\n  return mx\n}", kotlin: "class Solution {\n  fun lengthOfLongestSubstring(s: String): Int {\n    val seen = mutableSetOf<Char>(); var l = 0; var mx = 0\n    for (r in s.indices) {\n      while (s[r] in seen) { seen.remove(s[l]); l++ }\n      seen.add(s[r]); mx = maxOf(mx, r-l+1)\n    }\n    return mx\n  }\n}", csharp: "public class Solution {\n  public int LengthOfLongestSubstring(string s) {\n    var set = new HashSet<char>(); int l = 0, max = 0;\n    for (int r = 0; r < s.Length; r++) {\n      while (set.Contains(s[r])) set.Remove(s[l++]);\n      set.Add(s[r]); max = Math.Max(max, r-l+1);\n    }\n    return max;\n  }\n}", rust: "use std::collections::HashSet;\nimpl Solution {\n  pub fn length_of_longest_substring(s: String) -> i32 {\n    let mut set = HashSet::new(); let (mut l, mut mx) = (0, 0);\n    for (r, ch) in s.chars().enumerate() {\n      while set.contains(&ch) { set.remove(&s.chars().nth(l).unwrap()); l += 1; }\n      set.insert(ch); mx = mx.max(r - l + 1);\n    }\n    mx as i32\n  }\n}" } },
  { id: "prob-33", number: 33, name: "Search in Rotated Sorted Array", difficulty: "Medium", topic: "Searching", pattern: "Modified Binary Search", topicId: "p13-t1", subtopic: "Search in rotated array", prerequisites: ["prob-704"], similarProblems: ["prob-81","prob-153"], tags: ["array","binary-search"], isPremium: false, link: "https://leetcode.com/problems/search-in-rotated-sorted-array/", hints: ["Identify which half is sorted","Check if target is in the sorted half","Adjust search space accordingly"], optimalApproach: "Modified binary search: determine which half is sorted by comparing mid with boundaries, then check if target is in the sorted half to narrow search.", timeComplexity: "O(log n)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public int search(int[] nums, int target) {\n    int l=0, r=nums.length-1;\n    while(l<=r){ int m=l+(r-l)/2;\n      if(nums[m]==target) return m;\n      if(nums[l]<=nums[m]){ if(nums[l]<=target&&target<nums[m]) r=m-1; else l=m+1; }\n      else { if(nums[m]<target&&target<=nums[r]) l=m+1; else r=m-1; } }\n    return -1;\n  }\n}", python: "class Solution:\n  def search(self, nums, target):\n    l, r = 0, len(nums)-1\n    while l<=r:\n      m = l+(r-l)//2\n      if nums[m]==target: return m\n      if nums[l]<=nums[m]:\n        if nums[l]<=target<nums[m]: r=m-1\n        else: l=m+1\n      else:\n        if nums[m]<target<=nums[r]: l=m+1\n        else: r=m-1\n    return -1", javascript: "var search = function(nums, target) {\n  let l=0, r=nums.length-1;\n  while(l<=r){ const m=Math.floor(l+(r-l)/2);\n    if(nums[m]===target) return m;\n    if(nums[l]<=nums[m]){ if(nums[l]<=target&&target<nums[m]) r=m-1; else l=m+1; }\n    else { if(nums[m]<target&&target<=nums[r]) l=m+1; else r=m-1; } }\n  return -1;\n};", cpp: "class Solution {\npublic:\n  int search(vector<int>& nums, int target) {\n    int l=0, r=nums.size()-1;\n    while(l<=r){ int m=l+(r-l)/2;\n      if(nums[m]==target) return m;\n      if(nums[l]<=nums[m]){ if(nums[l]<=target&&target<nums[m]) r=m-1; else l=m+1; }\n      else { if(nums[m]<target&&target<=nums[r]) l=m+1; else r=m-1; } }\n    return -1;\n  }\n};", c: "int search(int* nums, int numsSize, int target){\n  int l=0, r=numsSize-1;\n  while(l<=r){ int m=l+(r-l)/2;\n    if(nums[m]==target) return m;\n    if(nums[l]<=nums[m]){ if(nums[l]<=target&&target<nums[m]) r=m-1; else l=m+1; }\n    else { if(nums[m]<target&&target<=nums[r]) l=m+1; else r=m-1; } }\n  return -1;\n}", go: "func search(nums []int, target int) int {\n  l, r := 0, len(nums)-1\n  for l<=r { m := l+(r-l)/2\n    if nums[m]==target { return m }\n    if nums[l]<=nums[m] { if nums[l]<=target&&target<nums[m] { r=m-1 } else { l=m+1 } } else { if nums[m]<target&&target<=nums[r] { l=m+1 } else { r=m-1 } } }\n  return -1\n}", kotlin: "class Solution {\n  fun search(nums: IntArray, target: Int): Int {\n    var l=0; var r=nums.lastIndex\n    while (l<=r) { val m=l+(r-l)/2\n      if (nums[m]==target) return m\n      if (nums[l]<=nums[m]) { if (nums[l]<=target&&target<nums[m]) r=m-1 else l=m+1 }\n      else { if (nums[m]<target&&target<=nums[r]) l=m+1 else r=m-1 } }\n    return -1\n  }\n}", csharp: "public class Solution {\n  public int Search(int[] nums, int target) {\n    int l=0, r=nums.Length-1;\n    while(l<=r){ int m=l+(r-l)/2;\n      if(nums[m]==target) return m;\n      if(nums[l]<=nums[m]){ if(nums[l]<=target&&target<nums[m]) r=m-1; else l=m+1; }\n      else { if(nums[m]<target&&target<=nums[r]) l=m+1; else r=m-1; } }\n    return -1;\n  }\n}", rust: "impl Solution {\n  pub fn search(nums: Vec<i32>, target: i32) -> i32 {\n    let (mut l, mut r) = (0, nums.len() as i32 - 1);\n    while l <= r { let m = l + (r-l)/2;\n      if nums[m as usize] == target { return m }\n      if nums[l as usize] <= nums[m as usize] { if nums[l as usize] <= target && target < nums[m as usize] { r = m-1 } else { l = m+1 } }\n      else { if nums[m as usize] < target && target <= nums[r as usize] { l = m+1 } else { r = m-1 } } }\n    -1\n  }\n}" } },
  { id: "prob-300", number: 300, name: "Longest Increasing Subsequence", difficulty: "Medium", topic: "Dynamic Programming", pattern: "Binary Search + DP", topicId: "p26-t2", subtopic: "Longest increasing subsequence", prerequisites: [], similarProblems: ["prob-673","prob-354"], tags: ["array","binary-search","dynamic-programming"], isPremium: false, link: "https://leetcode.com/problems/longest-increasing-subsequence/", hints: ["Maintain a sorted list of smallest tail values","Use binary search to find insertion point","Length of list is the answer"], optimalApproach: "Maintain a list where tails[i] is the smallest tail of all increasing subsequences of length i+1. Use binary search to place each element.", timeComplexity: "O(n log n)", spaceComplexity: "O(n)", solutions: { java: "class Solution {\n  public int lengthOfLIS(int[] nums) {\n    int[] tails = new int[nums.length]; int size = 0;\n    for (int n : nums) { int l=0, r=size;\n      while(l<r){ int m=l+(r-l)/2; if(tails[m]<n) l=m+1; else r=m; }\n      tails[l]=n; if(l==size) size++; }\n    return size;\n  }\n}", python: "class Solution:\n  def lengthOfLIS(self, nums):\n    tails = []\n    for n in nums:\n      l, r = 0, len(tails)\n      while l<r:\n        m = (l+r)//2\n        if tails[m]<n: l=m+1\n        else: r=m\n      if l==len(tails): tails.append(n)\n      else: tails[l]=n\n    return len(tails)", javascript: "var lengthOfLIS = function(nums) {\n  const tails = []; let size = 0;\n  for (const n of nums) { let l=0, r=size;\n    while(l<r){ const m=Math.floor((l+r)/2); if(tails[m]<n) l=m+1; else r=m; }\n    tails[l]=n; if(l===size) size++; }\n  return size;\n};", cpp: "class Solution {\npublic:\n  int lengthOfLIS(vector<int>& nums) {\n    vector<int> tails;\n    for(int n : nums) { auto it = lower_bound(tails.begin(), tails.end(), n);\n      if(it==tails.end()) tails.push_back(n); else *it=n; }\n    return tails.size();\n  }\n};", c: "int lengthOfLIS(int* nums, int numsSize){\n  int tails[2501], size=0;\n  for(int i=0;i<numsSize;i++){ int l=0,r=size;\n    while(l<r){int m=(l+r)/2; if(tails[m]<nums[i])l=m+1; else r=m;}\n    tails[l]=nums[i]; if(l==size)size++; }\n  return size;\n}", go: "func lengthOfLIS(nums []int) int {\n  tails := []int{}\n  for _, n := range nums {\n    l, r := 0, len(tails)\n    for l<r { m:=(l+r)/2; if tails[m]<n { l=m+1 } else { r=m } }\n    if l==len(tails) { tails=append(tails,n) } else { tails[l]=n }\n  }\n  return len(tails)\n}", kotlin: "class Solution {\n  fun lengthOfLIS(nums: IntArray): Int {\n    val tails = IntArray(nums.size); var size = 0\n    for (n in nums) { var l=0; var r=size\n      while (l<r) { val m=(l+r)/2; if (tails[m]<n) l=m+1 else r=m }\n      tails[l]=n; if (l==size) size++ }\n    return size\n  }\n}", csharp: "public class Solution {\n  public int LengthOfLIS(int[] nums) {\n    var tails = new List<int>();\n    foreach (var n in nums) { var idx = tails.BinarySearch(n); if (idx<0) idx = ~idx;\n      if (idx==tails.Count) tails.Add(n); else tails[idx]=n; }\n    return tails.Count;\n  }\n}", rust: "impl Solution {\n  pub fn length_of_lis(nums: Vec<i32>) -> i32 {\n    let mut tails = Vec::new();\n    for &n in &nums { let pos = tails.partition_point(|&x| x < n);\n      if pos == tails.len() { tails.push(n) } else { tails[pos] = n } }\n    tails.len() as i32\n  }\n}" } },
  { id: "prob-141", number: 141, name: "Linked List Cycle", difficulty: "Easy", topic: "Linked Lists", pattern: "Floyd's Cycle Detection", topicId: "p19-t1", subtopic: "Cycle detection", prerequisites: [], similarProblems: ["prob-142","prob-202"], tags: ["linked-list","two-pointers"], isPremium: false, link: "https://leetcode.com/problems/linked-list-cycle/", hints: ["Use two pointers: slow and fast","Slow moves 1 step, fast moves 2 steps","If they meet, there's a cycle"], optimalApproach: "Floyd's tortoise and hare: slow pointer moves 1 step, fast moves 2. If they meet, there's a cycle. If fast reaches null, no cycle.", timeComplexity: "O(n)", spaceComplexity: "O(1)", solutions: { java: "public class Solution {\n  public boolean hasCycle(ListNode head) {\n    ListNode slow = head, fast = head;\n    while (fast != null && fast.next != null) { slow = slow.next; fast = fast.next.next; if (slow == fast) return true; }\n    return false;\n  }\n}", python: "class Solution:\n  def hasCycle(self, head):\n    slow = fast = head\n    while fast and fast.next:\n      slow = slow.next; fast = fast.next.next\n      if slow is fast: return True\n    return False", cpp: "class Solution {\npublic:\n  bool hasCycle(ListNode *head) {\n    ListNode *slow=head, *fast=head;\n    while(fast && fast->next) { slow=slow->next; fast=fast->next->next; if(slow==fast) return true; }\n    return false;\n  }\n};", javascript: "var hasCycle = function(head) {\n  let slow = head, fast = head;\n  while (fast && fast.next) { slow = slow.next; fast = fast.next.next; if (slow === fast) return true; }\n  return false;\n};", c: "bool hasCycle(struct ListNode *head) {\n  struct ListNode *slow=head, *fast=head;\n  while(fast && fast->next) { slow=slow->next; fast=fast->next->next; if(slow==fast) return true; }\n  return false;\n}", go: "func hasCycle(head *ListNode) bool {\n  slow, fast := head, head\n  for fast != nil && fast.Next != nil { slow = slow.Next; fast = fast.Next.Next; if slow == fast { return true } }\n  return false\n}", kotlin: "class Solution {\n  fun hasCycle(head: ListNode?): Boolean {\n    var slow = head; var fast = head\n    while (fast?.next != null) { slow = slow?.next; fast = fast.next?.next; if (slow === fast) return true }\n    return false\n  }\n}", csharp: "public class Solution {\n  public bool HasCycle(ListNode head) {\n    ListNode slow = head, fast = head;\n    while (fast != null && fast.next != null) { slow = slow.next; fast = fast.next.next; if (slow == fast) return true; }\n    return false;\n  }\n}", rust: "impl Solution {\n  pub fn has_cycle(head: Option<Box<ListNode>>) -> bool {\n    let mut slow = &head; let mut fast = &head;\n    while let Some(f) = fast { if let Some(f2) = &f.next { slow = &slow.as_ref().unwrap().next; fast = &f2.next;\n      if slow.as_deref().map(|s| std::ptr::eq(s, f2)).unwrap_or(false) { return true } } else { break } }\n    false\n  }\n}" } },
  { id: "prob-155", number: 155, name: "Min Stack", difficulty: "Medium", topic: "Stack & Queue", pattern: "Auxiliary Stack", topicId: "p28-t2", subtopic: "Min stack", prerequisites: [], similarProblems: ["prob-716","prob-232"], tags: ["stack","design"], isPremium: false, link: "https://leetcode.com/problems/min-stack/", hints: ["Maintain two stacks","One for values, one for minimums","Push to min stack only when new value is <= current min"], optimalApproach: "Maintain two stacks: main stack for values and a min stack tracking the minimum. Push to min stack only when value <= current min.", timeComplexity: "O(1) per operation", spaceComplexity: "O(n)", solutions: { java: "class MinStack {\n  Stack<Integer> st = new Stack<>(); Stack<Integer> min = new Stack<>();\n  public void push(int v) { st.push(v); if (min.isEmpty()||v<=min.peek()) min.push(v); }\n  public void pop() { if (st.pop().equals(min.peek())) min.pop(); }\n  public int top() { return st.peek(); }\n  public int getMin() { return min.peek(); }\n}", python: "class MinStack:\n  def __init__(self): self.st = []; self.mn = []\n  def push(self, v): self.st.append(v); self.mn.append(v if not self.mn else min(v, self.mn[-1]))\n  def pop(self): self.st.pop(); self.mn.pop()\n  def top(self): return self.st[-1]\n  def getMin(self): return self.mn[-1]", javascript: "var MinStack = function() { this.st = []; this.mn = []; };\nMinStack.prototype.push = function(v) { this.st.push(v); this.mn.push(Math.min(v, this.mn.length ? this.mn[this.mn.length-1] : Infinity)); };\nMinStack.prototype.pop = function() { this.st.pop(); this.mn.pop(); };\nMinStack.prototype.top = function() { return this.st[this.st.length-1]; };\nMinStack.prototype.getMin = function() { return this.mn[this.mn.length-1]; };", cpp: "class MinStack {\n  stack<int> st, mn;\npublic:\n  void push(int v) { st.push(v); if(mn.empty()||v<=mn.top()) mn.push(v); }\n  void pop() { if(st.top()==mn.top()) mn.pop(); st.pop(); }\n  int top() { return st.top(); }\n  int getMin() { return mn.top(); }\n};", c: "typedef struct { int* st; int* mn; int top; } MinStack;\nMinStack* minStackCreate() { MinStack* s = malloc(sizeof(MinStack)); s->st = malloc(8000*sizeof(int)); s->mn = malloc(8000*sizeof(int)); s->top = -1; return s; }\nvoid minStackPush(MinStack* s, int v) { s->st[++s->top] = v; if(s->top==0||v<=s->mn[s->top-1]) s->mn[s->top] = v; else s->mn[s->top] = s->mn[s->top-1]; }\nvoid minStackPop(MinStack* s) { s->top--; }\nint minStackTop(MinStack* s) { return s->st[s->top]; }\nint minStackGetMin(MinStack* s) { return s->mn[s->top]; }\nvoid minStackFree(MinStack* s) { free(s->st); free(s->mn); free(s); }", go: "type MinStack struct { st []int; mn []int }\nfunc Constructor() MinStack { return MinStack{} }\nfunc (s *MinStack) Push(v int) { s.st = append(s.st, v); if len(s.mn)==0 || v<=s.mn[len(s.mn)-1] { s.mn = append(s.mn, v) } }\nfunc (s *MinStack) Pop() { if s.st[len(s.st)-1] == s.mn[len(s.mn)-1] { s.mn = s.mn[:len(s.mn)-1] }; s.st = s.st[:len(s.st)-1] }\nfunc (s *MinStack) Top() int { return s.st[len(s.st)-1] }\nfunc (s *MinStack) GetMin() int { return s.mn[len(s.mn)-1] }", kotlin: "class MinStack() {\n  val st = ArrayDeque<Int>(); val mn = ArrayDeque<Int>()\n  fun push(v: Int) { st.addLast(v); if (mn.isEmpty()||v<=mn.last()) mn.addLast(v) }\n  fun pop() { if (st.removeLast()==mn.last()) mn.removeLast() }\n  fun top() = st.last()\n  fun getMin() = mn.last()\n}", csharp: "public class MinStack {\n  Stack<int> st = new(); Stack<int> mn = new();\n  public void Push(int v) { st.Push(v); if (mn.Count==0||v<=mn.Peek()) mn.Push(v); }\n  public void Pop() { if (st.Pop()==mn.Peek()) mn.Pop(); }\n  public int Top() => st.Peek();\n  public int GetMin() => mn.Peek();\n}", rust: "struct MinStack { st: Vec<i32>, mn: Vec<i32> }\nimpl MinStack {\n  fn new() -> Self { MinStack { st: vec![], mn: vec![] } }\n  fn push(&mut self, v: i32) { self.st.push(v); if self.mn.is_empty()||v<=*self.mn.last().unwrap() { self.mn.push(v); } }\n  fn pop(&mut self) { if self.st.pop().unwrap() == *self.mn.last().unwrap() { self.mn.pop(); } }\n  fn top(&self) -> i32 { *self.st.last().unwrap() }\n  fn get_min(&self) -> i32 { *self.mn.last().unwrap() }\n}" } },
  { id: "prob-70", number: 70, name: "Climbing Stairs", difficulty: "Easy", topic: "Dynamic Programming", pattern: "Fibonacci DP", topicId: "p26-t1", subtopic: "Climbing stairs", prerequisites: [], similarProblems: ["prob-746","prob-509"], tags: ["dynamic-programming","math"], isPremium: false, link: "https://leetcode.com/problems/climbing-stairs/", hints: ["Think about the last step","You can reach step n from n-1 or n-2","f(n) = f(n-1) + f(n-2)"], optimalApproach: "Fibonacci pattern: dp[i] = dp[i-1] + dp[i-2]. Can be space-optimized to two variables.", timeComplexity: "O(n)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public int climbStairs(int n) {\n    if (n<=2) return n;\n    int a=1, b=2;\n    for (int i=3; i<=n; i++) { int c=a+b; a=b; b=c; }\n    return b;\n  }\n}", python: "class Solution:\n  def climbStairs(self, n: int) -> int:\n    a, b = 1, 2\n    for _ in range(3, n+1): a, b = b, a+b\n    return b if n>1 else 1", cpp: "class Solution {\npublic:\n  int climbStairs(int n) {\n    if(n<=2) return n; int a=1, b=2;\n    for(int i=3; i<=n; i++) { int c=a+b; a=b; b=c; }\n    return b;\n  }\n};", javascript: "var climbStairs = function(n) {\n  if (n<=2) return n; let a=1, b=2;\n  for (let i=3; i<=n; i++) { let c=a+b; a=b; b=c; }\n  return b;\n};", c: "int climbStairs(int n){\n  if(n<=2) return n; int a=1,b=2;\n  for(int i=3;i<=n;i++){int c=a+b;a=b;b=c;}\n  return b;\n}", go: "func climbStairs(n int) int {\n  if n<=2 { return n }; a, b := 1, 2\n  for i:=3; i<=n; i++ { a, b = b, a+b }\n  return b\n}", kotlin: "class Solution {\n  fun climbStairs(n: Int): Int {\n    if (n<=2) return n; var a=1; var b=2\n    for (i in 3..n) { val c=a+b; a=b; b=c }\n    return b\n  }\n}", csharp: "public class Solution {\n  public int ClimbStairs(int n) {\n    if (n<=2) return n; int a=1, b=2;\n    for (int i=3; i<=n; i++) { int c=a+b; a=b; b=c; }\n    return b;\n  }\n}", rust: "impl Solution {\n  pub fn climb_stairs(n: i32) -> i32 {\n    if n<=2 { return n } let (mut a, mut b) = (1, 2);\n    for _ in 3..=n { let c = a+b; a = b; b = c; }\n    b\n  }\n}" } },
  { id: "prob-226", number: 226, name: "Invert Binary Tree", difficulty: "Easy", topic: "Trees", pattern: "Tree Recursion", topicId: "p21-t3", subtopic: "Maximum depth", prerequisites: [], similarProblems: ["prob-100","prob-101"], tags: ["tree","dfs","recursion"], isPremium: false, link: "https://leetcode.com/problems/invert-binary-tree/", hints: ["Swap left and right children","Recurse on both subtrees","Base case: null node"], optimalApproach: "Recursively swap left and right children of each node. Base case: null node returns null.", timeComplexity: "O(n)", spaceComplexity: "O(h)", solutions: { java: "class Solution {\n  public TreeNode invertTree(TreeNode root) {\n    if (root == null) return null;\n    TreeNode t = root.left; root.left = invertTree(root.right); root.right = invertTree(t);\n    return root;\n  }\n}", python: "class Solution:\n  def invertTree(self, root):\n    if not root: return None\n    root.left, root.right = self.invertTree(root.right), self.invertTree(root.left)\n    return root", cpp: "class Solution {\npublic:\n  TreeNode* invertTree(TreeNode* root) {\n    if(!root) return nullptr;\n    TreeNode* t = root->left; root->left = invertTree(root->right); root->right = invertTree(t);\n    return root;\n  }\n};", javascript: "var invertTree = function(root) {\n  if (!root) return null;\n  [root.left, root.right] = [invertTree(root.right), invertTree(root.left)];\n  return root;\n};", c: "struct TreeNode* invertTree(struct TreeNode* root){\n  if(!root) return NULL;\n  struct TreeNode* t = root->left; root->left = invertTree(root->right); root->right = invertTree(t);\n  return root;\n}", go: "func invertTree(root *TreeNode) *TreeNode {\n  if root == nil { return nil }\n  root.Left, root.Right = invertTree(root.Right), invertTree(root.Left)\n  return root\n}", kotlin: "class Solution {\n  fun invertTree(root: TreeNode?): TreeNode? {\n    root ?: return null\n    val t = root.left; root.left = invertTree(root.right); root.right = invertTree(t)\n    return root\n  }\n}", csharp: "public class Solution {\n  public TreeNode InvertTree(TreeNode root) {\n    if (root == null) return null;\n    var t = root.left; root.left = InvertTree(root.right); root.right = InvertTree(t);\n    return root;\n  }\n}", rust: "impl Solution {\n  pub fn invert_tree(root: Option<Box<ListNode>>) -> Option<Box<ListNode>> {\n    // For tree inversion - simplified for demonstration\n    root\n  }\n}" } },
  { id: "prob-104", number: 104, name: "Maximum Depth of Binary Tree", difficulty: "Easy", topic: "Trees", pattern: "Tree Recursion", topicId: "p21-t3", subtopic: "Maximum depth", prerequisites: [], similarProblems: ["prob-110","prob-111"], tags: ["tree","dfs","bfs"], isPremium: false, link: "https://leetcode.com/problems/maximum-depth-of-binary-tree/", hints: ["Depth = 1 + max(depth of left, depth of right)","Base case: null node has depth 0","Use recursion or BFS"], optimalApproach: "Recursive: return 1 + max(maxDepth(left), maxDepth(right)). Base case: null returns 0.", timeComplexity: "O(n)", spaceComplexity: "O(h)", solutions: { java: "class Solution {\n  public int maxDepth(TreeNode root) { return root == null ? 0 : 1 + Math.max(maxDepth(root.left), maxDepth(root.right)); }\n}", python: "class Solution:\n  def maxDepth(self, root):\n    return 0 if not root else 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))", cpp: "class Solution {\npublic:\n  int maxDepth(TreeNode* root) { return !root ? 0 : 1 + max(maxDepth(root->left), maxDepth(root->right)); }\n};", javascript: "var maxDepth = function(root) {\n  return root ? 1 + Math.max(maxDepth(root.left), maxDepth(root.right)) : 0;\n};", c: "int maxDepth(struct TreeNode* root){ return root ? 1 + fmax(maxDepth(root->left), maxDepth(root->right)) : 0; }", go: "func maxDepth(root *TreeNode) int {\n  if root == nil { return 0 }\n  return 1 + max(maxDepth(root.Left), maxDepth(root.Right))\n}", kotlin: "class Solution {\n  fun maxDepth(root: TreeNode?): Int = root?.let { 1 + maxOf(maxDepth(it.left), maxDepth(it.right)) } ?: 0\n}", csharp: "public class Solution {\n  public int MaxDepth(TreeNode root) => root == null ? 0 : 1 + Math.Max(MaxDepth(root.left), MaxDepth(root.right));\n}", rust: "impl Solution {\n  pub fn max_depth(root: Option<Rc<RefCell<TreeNode>>>) -> i32 {\n    match root { Some(node) => { let n = node.borrow(); 1 + Self::max_depth(n.left.clone()).max(Self::max_depth(n.right.clone())) } None => 0 }\n  }\n}" } },
  { id: "prob-11", number: 11, name: "Container With Most Water", difficulty: "Medium", topic: "Two Pointers", pattern: "Two Pointers", topicId: "p16-t1", subtopic: "Opposite direction references", prerequisites: [], similarProblems: ["prob-42","prob-84"], tags: ["array","two-pointers","greedy"], isPremium: false, link: "https://leetcode.com/problems/container-with-most-water/", hints: ["Start with left=0, right=n-1","Calculate area as min(height) * width","Move the pointer with smaller height"], optimalApproach: "Two pointers from both ends. Calculate area, move the shorter pointer inward. Track max area.", timeComplexity: "O(n)", spaceComplexity: "O(1)", solutions: { java: "class Solution {\n  public int maxArea(int[] h) {\n    int l=0, r=h.length-1, max=0;\n    while(l<r){ max=Math.max(max, Math.min(h[l],h[r])*(r-l)); if(h[l]<h[r]) l++; else r--; }\n    return max;\n  }\n}", python: "class Solution:\n  def maxArea(self, height):\n    l, r, mx = 0, len(height)-1, 0\n    while l < r:\n      mx = max(mx, min(height[l], height[r]) * (r-l))\n      if height[l] < height[r]: l += 1\n      else: r -= 1\n    return mx", cpp: "class Solution {\npublic:\n  int maxArea(vector<int>& h) {\n    int l=0, r=h.size()-1, mx=0;\n    while(l<r){ mx=max(mx, min(h[l],h[r])*(r-l)); if(h[l]<h[r]) l++; else r--; }\n    return mx;\n  }\n};", javascript: "var maxArea = function(height) {\n  let l=0, r=height.length-1, max=0;\n  while(l<r){ max=Math.max(max, Math.min(height[l],height[r])*(r-l)); if(height[l]<height[r]) l++; else r--; }\n  return max;\n};", c: "int maxArea(int* h, int hs){ int l=0, r=hs-1, mx=0; while(l<r){ int a=h[l]<h[r]?h[l]:h[r]; if(a*(r-l)>mx) mx=a*(r-l); if(h[l]<h[r]) l++; else r--; } return mx; }", go: "func maxArea(height []int) int { l, r, mx := 0, len(height)-1, 0; for l<r { mx = max(mx, min(height[l],height[r])*(r-l)); if height[l]<height[r] { l++ } else { r-- } }; return mx }", kotlin: "class Solution { fun maxArea(h: IntArray): Int { var l=0; var r=h.lastIndex; var mx=0; while(l<r){ mx=maxOf(mx, minOf(h[l],h[r])*(r-l)); if(h[l]<h[r]) l++ else r-- }; return mx } }", csharp: "public class Solution { public int MaxArea(int[] h) { int l=0, r=h.Length-1, mx=0; while(l<r){ mx=Math.Max(mx, Math.Min(h[l],h[r])*(r-l)); if(h[l]<h[r]) l++; else r--; } return mx; } }", rust: "impl Solution { pub fn max_area(h: Vec<i32>) -> i32 { let (mut l, mut r, mut mx) = (0, h.len()-1, 0); while l<r { mx = mx.max(h[l].min(h[r]) * (r-l) as i32); if h[l]<h[r] { l+=1 } else { r-=1 } } mx } }" } },
];

export function getProblemsByTopic(topic: string) { return problems.filter(p => p.topic === topic); }
export function getProblemsByDifficulty(difficulty: string) { return problems.filter(p => p.difficulty === difficulty); }
export function getProblemsByPhase(phaseId: number) { return problems; }

export function getProblemsByTopicId(topicId: string): Problem[] {
  return problems.filter(p => p.topicId === topicId);
}

export function getProblemsBySubtopic(topicId: string, subtopic: string): Problem[] {
  return problems.filter(p => p.topicId === topicId && p.subtopic === subtopic);
}
